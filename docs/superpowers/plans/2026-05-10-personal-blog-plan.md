# Personal Blog Homepage Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a personal blog homepage using Jekyll + chirpy theme, deployable to GitHub Pages.

**Architecture:** Jekyll static site with chirpy gem-based theme. Fixed left sidebar (avatar, intro, social links, nav) + right content area with paginated article list. About / Archive / Tags / Categories pages auto-generated by chirpy from `_tabs/` markdown files.

**Tech Stack:** Ruby 3.x, Jekyll 4.x, jekyll-theme-chirpy ~7.2, GitHub Actions, GitHub Pages

---

## File Map

| File | Purpose |
|------|---------|
| `Gemfile` | Ruby dependencies (jekyll, chirpy) |
| `_config.yml` | Site configuration (title, lang, social, theme) |
| `index.html` | Home page entry point (delegates to chirpy home layout) |
| `_tabs/about.md` | About me page content |
| `_tabs/archives.md` | Article archives page |
| `_tabs/tags.md` | Tag cloud page |
| `_tabs/categories.md` | Category listing page |
| `_data/contact.yml` | Social links displayed in sidebar |
| `_data/share.yml` | Share button platforms for posts |
| `_posts/2026-05-10-hello-world.md` | First sample blog post |
| `assets/css/jekyll-theme-chirpy.scss` | Theme CSS overrides (chirpy convention) |
| `_plugins/posts-lastmod-hook.rb` | Auto-set last modified dates from git |
| `.github/workflows/pages-deploy.yml` | GitHub Actions CI/CD |
| `.gitignore` | Git ignore rules |

---

### Task 1: Initialize Project Structure

**Files:**
- Create: `Gemfile`
- Create: `.gitignore`
- Create: `index.html`

- [ ] **Step 1: Create project directories**

Run:
```bash
cd /Users/xingrui/claudemain
mkdir -p _tabs _data _posts _plugins assets/css assets/img .github/workflows
```

- [ ] **Step 2: Create Gemfile**

Write `Gemfile`:

```ruby
source "https://rubygems.org"

gem "jekyll-theme-chirpy", "~> 7.2", ">= 7.2.4"

gem "html-proofer", "~> 5.0", group: :test

platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end

gem "wdm", "~> 0.2.0", platforms: [:mingw, :x64_mingw, :mswin]
```

- [ ] **Step 3: Create .gitignore**

Write `.gitignore`:

```
_site
.jekyll-cache
.jekyll-metadata
vendor
Gemfile.lock
.DS_Store
```

- [ ] **Step 4: Create index.html**

Write `index.html`:

```html
---
layout: home
---
```

- [ ] **Step 5: Install dependencies**

Run:
```bash
cd /Users/xingrui/claudemain
bundle install
```

Expected: Gems installed successfully, `Gemfile.lock` created.

- [ ] **Step 6: Commit**

```bash
cd /Users/xingrui/claudemain
git init
git add Gemfile Gemfile.lock .gitignore index.html
git commit -m "chore: initialize Jekyll project with chirpy theme"
```

---

### Task 2: Configure _config.yml

**Files:**
- Create: `_config.yml`

- [ ] **Step 1: Create _config.yml**

Write `_config.yml`:

```yaml
# ---- Theme ----
theme: jekyll-theme-chirpy
lang: zh-CN
timezone: Asia/Shanghai

# ---- Site Info ----
title: 你的名字
tagline: 简短的个人标语
description: >-
  一段个人简介，会出现在 SEO meta 和侧边栏中。
  可以写多行。

url: ""
baseurl: ""

# ---- Appearance ----
avatar: /assets/img/avatar.jpg
theme_mode: light

# ---- Social ----
social:
  name: 你的名字
  email: you@example.com
  links:
    - https://github.com/yourusername

# ---- Page Settings ----
page_views: false         # Disable PV counter (no 3rd-party script)
reading_time: true        # Show estimated reading time
back_to_top: true         # Show back-to-top button
toc: true                 # Table of contents in posts

# ---- Pagination ----
paginate: 10
```

- [ ] **Step 2: Verify config loads**

Run:
```bash
cd /Users/xingrui/claudemain
bundle exec jekyll config --config _config.yml 2>&1 | head -5
```

Expected: No errors, prints config hash.

- [ ] **Step 3: Commit**

```bash
git add _config.yml
git commit -m "feat: add site configuration"
```

---

### Task 3: Create Navigation Tabs

**Files:**
- Create: `_tabs/about.md`
- Create: `_tabs/archives.md`
- Create: `_tabs/tags.md`
- Create: `_tabs/categories.md`

- [ ] **Step 1: Create about page**

Write `_tabs/about.md`:

```markdown
---
title: 关于我
icon: fas fa-user
order: 4
---

你好，我是 [你的名字]。这里是我的个人博客，用来记录和分享我在技术、生活中的所思所想。

## 技能

- 前端开发 / 后端开发 / 全栈
- (在此补充你的技能)

## 联系方式

欢迎通过侧边栏的社交链接联系我，或者发送邮件到 you@example.com。
```

- [ ] **Step 2: Create archives page**

Write `_tabs/archives.md`:

```markdown
---
title: 归档
icon: fas fa-archive
order: 3
---
```

- [ ] **Step 3: Create tags page**

Write `_tabs/tags.md`:

```markdown
---
title: 标签
icon: fas fa-tags
order: 2
---
```

- [ ] **Step 4: Create categories page**

Write `_tabs/categories.md`:

```markdown
---
title: 分类
icon: fas fa-folder
order: 1
---
```

- [ ] **Step 5: Commit**

```bash
git add _tabs/
git commit -m "feat: add navigation tabs (about, archives, tags, categories)"
```

---

### Task 4: Set Up Data Files

**Files:**
- Create: `_data/contact.yml`
- Create: `_data/share.yml`
- Create: `_plugins/posts-lastmod-hook.rb`

- [ ] **Step 1: Create contact.yml**

Write `_data/contact.yml`:

```yaml
- type: github
  icon: fab fa-github
  url: "https://github.com/yourusername"

- type: email
  icon: fas fa-envelope
  url: "mailto:you@example.com"

- type: twitter
  icon: fab fa-twitter
  url: "https://twitter.com/yourusername"

- type: rss
  icon: fas fa-rss
  noblank: true
```

- [ ] **Step 2: Create share.yml**

Write `_data/share.yml`:

```yaml
platforms:
  - type: Twitter
    icon: fab fa-twitter
    link: "https://twitter.com/intent/tweet?text=TITLE&url=URL"

  - type: Facebook
    icon: fab fa-facebook
    link: "https://www.facebook.com/sharer/sharer.php?title=TITLE&url=URL"

  - type: Telegram
    icon: fab fa-telegram
    link: "https://t.me/share/url?url=URL&text=TITLE"
```

- [ ] **Step 3: Create posts-lastmod-hook.rb**

Write `_plugins/posts-lastmod-hook.rb`:

```ruby
Jekyll::Hooks.register :posts, :post_init do |post|
  commit_num = `git rev-list --count HEAD "#{post.path}"`
  if commit_num.to_i > 1
    lastmod_date = `git log -1 --pretty="%ad" --date=iso "#{post.path}"`
    post.data['last_modified_at'] = lastmod_date
  end
end
```

- [ ] **Step 4: Commit**

```bash
git add _data/ _plugins/
git commit -m "feat: add contact data, share config, and lastmod hook"
```

---

### Task 5: Set Up Custom Styles

**Files:**
- Create: `assets/css/jekyll-theme-chirpy.scss`

- [ ] **Step 1: Create SCSS file**

Write `assets/css/jekyll-theme-chirpy.scss`:

```scss
---
---

@import 'jekyll-theme-chirpy';

// ---------- Custom overrides ----------

// Example: adjust sidebar width
// #sidebar { width: 280px; }
// #main-wrapper { margin-left: 280px; }

// Example: change link color
// :root {
//   --link-color: #2a5db0;
//   --link-underline-color: #2a5db0;
// }

// Add your custom styles below
```

- [ ] **Step 2: Create placeholder avatar**

Run:
```bash
cd /Users/xingrui/claudemain
# Create a 200x200 placeholder PNG if no real avatar available
echo "Placeholder avatar - replace with your own image" > assets/img/avatar.txt
```

Note: User should replace this with a real `avatar.jpg` (200×200 recommended).

- [ ] **Step 3: Commit**

```bash
git add assets/
git commit -m "feat: add custom styles entry point and avatar placeholder"
```

---

### Task 6: Create First Sample Blog Post

**Files:**
- Create: `_posts/2026-05-10-hello-world.md`

- [ ] **Step 1: Create hello-world post**

Write `_posts/2026-05-10-hello-world.md`:

```markdown
---
title: Hello World — 我的第一篇博客
author: 你的名字
date: 2026-05-10 10:00:00 +0800
categories: [随笔]
tags: [blog, hello]
pin: true
---

欢迎来到我的个人博客！

## 关于这个博客

这个博客使用 [Jekyll](https://jekyllrb.com/) 搭建，主题是 [Chirpy](https://github.com/cotes2020/jekyll-theme-chirpy)，托管在 GitHub Pages 上。

## 我会写什么

这里会记录我在技术探索、项目实践中的思考和总结。具体来说：

- 技术踩坑与解决方案
- 项目经验总结
- 读书笔记
- 偶尔的生活随笔

## 开始

写博客的初衷是 **记录与分享**。希望这里的内容能对你有所帮助。

感谢来访！
```

- [ ] **Step 2: Verify Jekyll can build the site**

Run:
```bash
cd /Users/xingrui/claudemain
bundle exec jekyll build 2>&1
```

Expected: Build successful. `_site/` directory created with HTML files. No errors.

- [ ] **Step 3: Start local dev server and verify**

Run:
```bash
bundle exec jekyll serve --host 0.0.0.0
```

Access `http://localhost:4000` in browser. Verify:
- Left sidebar shows avatar, title, tagline, social links, nav
- Right content area shows the hello-world post
- Clicking About / Archives / Tags / Categories tabs works
- Post shows categories and tags

- [ ] **Step 4: Commit**

```bash
git add _posts/
git commit -m "feat: add first hello-world blog post"
```

---

### Task 7: Set Up GitHub Actions Deployment

**Files:**
- Create: `.github/workflows/pages-deploy.yml`

- [ ] **Step 1: Create GitHub Actions workflow**

Write `.github/workflows/pages-deploy.yml`:

```yaml
name: "Build and Deploy"

on:
  push:
    branches:
      - main
    paths-ignore:
      - .gitignore
      - README.md
      - LICENSE

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true

      - name: Build site
        run: bundle exec jekyll b -d "_site${{ steps.pages.outputs.base_path }}"
        env:
          JEKYLL_ENV: "production"

      - name: Test site
        run: |
          bundle exec htmlproofer _site \
            --disable-external \
            --ignore-urls "/^http:\/\/127.0.0.1/,/^http:\/\/0.0.0.0/,/^http:\/\/localhost/"

      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "_site${{ steps.pages.outputs.base_path }}"

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 2: Commit**

```bash
git add .github/
git commit -m "feat: add GitHub Actions deploy workflow"
```

---

### Task 8: Git Init and Full Build Verification

**Files:** None new (verify only)

- [ ] **Step 1: Verify full file tree**

Run:
```bash
cd /Users/xingrui/claudemain
find . -not -path './.git/*' -not -path './_site/*' -not -path './vendor/*' -not -path './.bundle/*' -not -name 'Gemfile.lock' | sort
```

Expected output:

```
.
./.gitignore
./.github
./.github/workflows
./.github/workflows/pages-deploy.yml
./Gemfile
./_config.yml
./_data
./_data/contact.yml
./_data/share.yml
./_plugins
./_plugins/posts-lastmod-hook.rb
./_posts
./_posts/2026-05-10-hello-world.md
./_tabs
./_tabs/about.md
./_tabs/archives.md
./_tabs/categories.md
./_tabs/tags.md
./assets
./assets/css
./assets/css/jekyll-theme-chirpy.scss
./assets/img
./assets/img/avatar.txt
./index.html
```

- [ ] **Step 2: Clean build from scratch**

Run:
```bash
cd /Users/xingrui/claudemain
rm -rf _site
bundle exec jekyll build
```

Expected: Clean build, no warnings, no errors.

- [ ] **Step 3: Verify generated _site structure**

Run:
```bash
ls _site/
ls _site/about/ _site/archives/ _site/tags/ _site/categories/ 2>&1
ls _site/posts/hello-world/ 2>&1
```

Expected: Each page path exists with `index.html`.

- [ ] **Step 4: Commit any remaining files**

```bash
git status
git add -A
git commit -m "chore: finalize project setup" 2>&1
```

---

## After Implementation

1. Replace `assets/img/avatar.txt` with a real `avatar.jpg` (200×200)
2. Update `_config.yml` with real name, email, GitHub URL
3. Update `_tabs/about.md` with real bio and skills
4. Update `_data/contact.yml` with real social links
5. Create GitHub repo `<username>.github.io` and push: `git remote add origin <url> && git push -u origin main`
6. Enable GitHub Pages in repo Settings → Pages → Source: GitHub Actions
7. Site live at `https://<username>.github.io`
